## Summary
- Repo standards + CI workflow

## Changes
- Added .editorconfig, .prettierrc, .eslint.config.js, .nvmrc, .gitignore
- Added GitHub Actions CI to build each app/server (non-fatal if no build script)

## Notes
- No existing code modified; only added standards/CI files.
